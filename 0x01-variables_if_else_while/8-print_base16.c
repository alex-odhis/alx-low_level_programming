#include <stdio.h>
/**
 * main - prints numbers of base 16 followed by newline
 * Return: 0 always
 */
int main(void)
{
	int n;
	char ch;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
		for (ch < 'a'; ch < f; ch++)
		{
			putchar(ch)
		}
		putchar('\n');
		return (0);
	}
}
